import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sleepmaestro.sleepmaestro',
  appName: 'Sleep-Maestro',
  webDir: 'www'
};

export default config;
